// src/model/derivedStats.js — post-Phase-1 dependency seam.
// Contract-first stub: tools/derivedstats.mjs is committed red against this.

const pending = () => { throw new Error('derived-stat contract not implemented'); };

export const derivedStatRuleProblems = pending;
export const resolveDerivedStatRules = pending;
export const deriveStat = pending;
export const createDerivedStatRuleSnapshot = pending;
export const restoreDerivedStatRuleSnapshot = pending;

