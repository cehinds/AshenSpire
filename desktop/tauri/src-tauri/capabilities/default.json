{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike capability: core defaults for the main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
