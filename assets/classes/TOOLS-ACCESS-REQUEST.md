# AS-HD-040 — decision request: who remodels three class figures

**To:** IT Manager III · **From:** `lease-AS-HD-040-maker` · **Date:** 2026-09-02
**Authority needed:** a path assignment for `tools/sprites-blender.py`.
**This is a request, not a claim.** Nothing in `tools/**` has been edited.

## EVIDENCE

Checked against `origin/dev`, not this seat's clone — a lease claim from this
seat was withdrawn once before for exactly that reason.

1. **Owner ruling, 2026-09-02.** Of the four frozen crops, only **rogue** carries
   the approved look. **reaver, starseer and herald must be remodelled to match
   it.** The hub's *Proof Accepted · 20/20 gates · findings 0* must no longer be
   read as approving the look of all four.
2. **The ruling is independently reproducible.** `check-look-conformance.mjs`
   scores each asset against an envelope re-measured from the pinned rogue blob:
   rogue 0 traits off, reaver 2, herald 4, starseer 5. The measurement was not
   told the answer and agrees with the owner's eye.
3. **The approved look has no builder.** `tools/sprites-blender.py` defines
   `build_reaver`, `build_starseer`, `build_herald`. There is no `build_rogue`
   anywhere in the repository, and `tools/equipment-blender.py:408` maps
   `"rogue": lib["build_reaver"]` — the original silhouette defect.
4. **The packet came from outside this repository.** The pipeline renders classes
   at 450×570 in WEBP. Nothing under `tools/` emits 512×512, and nothing emits
   PNG. The four crops are 512×512 PNG. So `SUCCESSOR-CONTRACT.md` §5.1 is not a
   paperwork gap — the source is not here, and rogue cannot currently be
   regenerated by anyone.
5. **`tools/**` is role-owned but unleased.** `git-ownership.json` assigns it to
   `owner_role: it-support`, lane `support-tooling`.
   `lease-AS-HD-057-it-support` is `revoked: true`; the live
   `lease-AS-HD-057-it-support-r2` holds only `.agentops/work/AS-HD-057/**` and
   `.agentops/events/AS-HD-057/**`. **No active writer lease claims `tools/**`.**
6. **The ownership row points both ways.** Its own note reads *"Repo tooling and
   support scripts. Restores the ability to work; product behaviour changes
   belong to maker."* A class look remodel is a product behaviour change living
   in a support-owned path.

## OPTIONS

**O1 — Extend `lease-AS-HD-040-maker` to `tools/sprites-blender.py`.**
The remodel is a product behaviour change, which the ownership row already
assigns to maker; the path is unleased, so nothing is contested. This seat has
the measured target, the checker, and a drafted `build_rogue`.
*Cost:* a maker writing in a support-owned lane, against the lane's letter.

**O2 — Assign the work to an `it-support` seat.**
Follows `owner_role` literally. That seat inherits the measured envelope and the
checker and does not need to re-derive the art direction.
*Cost:* the lane exists to *restore the ability to work*, and its own note
excludes product behaviour changes. It would be doing maker work under a support
lease, and the round trip costs a re-seat.

**O3 — Split it: `build_rogue` first under O1, then rule on the three remodels.**
Landing `build_rogue` makes the approved look reproducible and proves the
procedural pipeline can reach it before anyone commits to three rewrites. The
larger question waits on evidence rather than on argument.
*Cost:* two decisions instead of one.

## RECOMMENDATION

**O3.** Today the approved look is 80,807 bytes nobody can regenerate; every
plan downstream assumes rogue is a stable target and it is not. `build_rogue` is
one function against a fixed, measurable gate — if the pipeline cannot reach the
approved look, that changes what the three remodels should even be, and it is
much cheaper to learn now.

O1 is the right long-run assignment if the ruling in §6 is that product
behaviour outranks the lane. O2 is defensible but pays a re-seat to move work to
a lane whose own note excludes it.

## NEXT (smallest action)

Extend `lease-AS-HD-040-maker` to `tools/sprites-blender.py`, scoped to adding
`build_rogue` and its two materials plus the `builders` map entry — no edits to
the three existing builders under this grant.

Then this seat renders, scores against `check-look-conformance.mjs`, iterates the
materials until it passes, and returns the receipt. If it cannot be made to pass,
that is the finding and it comes back here before any remodel starts.

## AUTH

- **Needed:** the path assignment above. Nothing else.
- **Not requested:** push, PR, merge, adoption, integration, deployment, or
  release. Decision D1 authorises none of these and this request does not ask
  for them.
- **Not this seat's:** the QA verdict on candidate `a72cac96`, and the standing
  hero-accent question below.

## OPEN — carried, not resolved

Every class carries a broad `ACCENT_CLOTH` sweep so *"the silhouette that glows
is always yours"*. The approved look measures gold at **0.6 %** — six times less
than herald. `build_rogue.proposal.py` keeps the accent as a narrow mantle edge
and a throat clasp, which is a guess. **Whether the hero-accent convention
outranks the approved look decides all four figures, not just rogue**, and it is
not this seat's call.
