# content/source/equipmentGrants.csv — cards a piece carries with it (Ashen Spire)
#
# The payload half of the `bound` tag. A piece that carries cards into the
# starting deck declares TWO things, and the validator holds them together:
#
#   1. the tag  — `bound` in its tags column (weapons.csv / outfits.csv).
#      This is the PROMISE, and it is the half the player can see.
#   2. a row here — the CARDS. This is the payload.
#
# Tagged with no row is a broken promise; a row with no tag is a silent grant.
# validateEquipment refuses both by name, so neither half can drift alone.
#
# WHY SPUN OUT rather than a `grantsCards` column on weapons.csv: the same
# reason cardTagging.csv is not a column on the card — one association table
# beats the same field repeated across every piece kind, and a new piece kind
# joins by carrying the tag rather than by growing a schema.
#
# Cards land as GRANTS in the composed starting deck: one copy each, dealt
# before filler, never dropped (balance.equipment.startingDeck). Every id must
# exist in the card registry.
#
# sourceId  the piece id, exactly as written in weapons.csv or outfits.csv
# cards     '|'-separated card ids
#
# Empty by design. The mechanism is live and inert until content authors a
# piece into it — nothing here invents a grant to make the seam look used.
sourceId,cards
