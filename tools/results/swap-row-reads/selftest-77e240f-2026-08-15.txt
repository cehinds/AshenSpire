swap-row-reads --selftest — each known-bad planted as BYTES in a copy of the REAL tree,
the two CSV rows authored into the same copy, and the real content-build run in between.
Scored against an UNPLANTED CONTROL, because this tree is already red and exit 1 is free.

  CONTROL  exit 1 · 390x844/flat reports 2 rows, li 3 = head 1 + note 2
  CONTROL findings already present: 6

  CAUGHT  the comparison stops wrapping — one long note bleeds the column sideways (LAW 5)
      planted:yes control:clean exit 1 · table rows 2, li 2 = head 1 + note 1 (control li 3 = head 1 + note 2)
  CAUGHT  the summary loses its tap floor (LAW 4)
      planted:yes control:clean exit 1 · table rows 2, li 3 = head 1 + note 2 (control li 3 = head 1 + note 2)
  CAUGHT  the note stops being rendered at all — the declined +2 goes silent again
      planted:yes control:clean exit 1 · table rows 2, li 1 = head 1 + note 0 (control li 3 = head 1 + note 2)
  CAUGHT  the sibling's declaration copied literally — grid tears the price off its own arrow
      planted:yes control:clean exit 1 · table rows 2, li 4 = head 2 + note 2 (control li 3 = head 1 + note 2)
      ALSO ON THIS RUN: READS AS ONE WORD (the adjacency check) is SILENT — it cannot see this defect, which is why the check above exists.
  CAUGHT  every slot prices, not only the combat-swappable ones — two rows becomes four
      rows 4 (control 2, expected 4)

  5/5 CAUGHT.
  BOUNDARY: 5 defect(s) on one surface, each scored against an unplanted control,
  and each scored on the FINDINGS only — never on this tool's own legend, which now names its checks.
  Silent about every defect nobody thought to plant, and about the 6 finding(s) the
  control already carries — those are OBSERVED on the real tree and need no plant.
